# Copy this file to .env (gitignored) and fill in real values before running `docker compose up`.
# Never commit the real .env file.

POSTGRES_PASSWORD=change-me
# Generate with: openssl rand -base64 48
JWT_KEY=change-me
